services:
  flashforge-slicer:
    build: .
    image: flashforge-slicer:latest
    container_name: flashforge-slicer
    restart: unless-stopped
    ports:
      - "8099:8099"
    volumes:
      - ./data:/data   # settings and saved printer
